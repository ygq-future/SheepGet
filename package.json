{
  "name": "sheep-get-workspace",
  "private": true,
  "scripts": {
    "check:go:fmt": "bash -c 'test -z \"$(gofmt -l (git ls-files \"*.go\"))\"'",
    "check:go:vet": "go vet ./...",
    "check:go:test": "go test ./...",
    "check:frontend:typecheck": "bun --cwd frontend run typecheck",
    "check:frontend:lint": "bun --cwd frontend run lint",
    "check:frontend:format": "bun --cwd frontend run format:check",
    "quality": "node scripts/quality-gate.mjs"
  }
}
