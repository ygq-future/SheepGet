## Parent

https://github.com/ygq-future/SheepGet/issues/1

## What to build

将应用底层桌面框架从 Wails v2 平滑升级到 Wails v3 Beta (`v3.0.0-beta.20`)，重构主入口生命周期为程序化架构，解耦 Service 绑定，适配前端运行时与门禁工具链，为后续原生多窗口与配置中心奠定基座，同时保证已有下载引擎与界面功能完整可用、测试全绿无回归。

## Acceptance criteria

- [x] 升级 Go 依赖至 `github.com/wailsapp/wails/v3 v3.0.0-beta.20`，移除 v2 冗余依赖并完成模块校验。
- [x] 主入口迁移至 Wails v3 程序化生命周期（`application.New`），并将后端能力注册为解耦的独立服务（`application.Service`）。
- [x] 前端运行时接入 `@wailsio/runtime`，适配新版 `frontend/bindings` 生成路径并替换原有导入。
- [x] 门禁脚本 `scripts/quality-gate.mjs` 适配 `wails3` CLI 构建与校验流程。
- [x] 现有下载核心（单流与动态分块、Range 响应验证、慢区块协助、队列管理与持久化恢复）保持完全完好。
- [x] 现有所有测试（Go test、Bun test）与全量质量门禁（Lint、Typecheck、Build、Prettier、commitlint）通过，无回归问题。

## Blocked by

None (can start immediately)
