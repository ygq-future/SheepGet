# SheepGet

SheepGet 的下载交互领域术语。

## Language

**文件信息对话框**：
展示待下载资源的信息，并允许用户修改文件名、保存位置及确认下载的对话框。
_Avoid_: 文件信息面板

**显示文件信息时立即下载**：
允许文件信息对话框保持打开时在后台开始下载的设置；用户仍可编辑保存信息并确认下载。

**媒体悬浮条**：
网页中关联可下载媒体资源的紧凑操作入口，用户取消下载后仍保持可用，并可手动关闭。

**暂停任务**：
停止传输但仍保留在下载列表中的任务，包括提前下载后用户取消文件信息对话框所保留的任务。

**内置分类**：
软件预置的文件归类规则与保存目录配置，其后缀规则可与自定义分类重叠。

**自定义分类**：
用户创建并配置后缀规则及保存目录的文件分类，匹配优先级高于内置分类；按用户可拖动调整的列表顺序取首个匹配项，新增分类置顶。

**重复链接**：
与已有下载任务的完整 URL 相同的链接。

**同名文件冲突**：
新下载的目标保存位置已存在同名文件的情况。

**下载进度窗口**：
独立于主界面、集中展示多个任务实时下载区块和进度的共享窗口；每个任务完成后，其展示区域转为完成信息。

**完成信息**：
下载进度窗口中已完成任务的展示区域，提供打开文件、打开文件夹和关闭该区域的操作。

**处理失败**：
媒体分片已下载，但生成成品文件的合并处理失败的任务状态；保留分片以供重试处理。

**重试处理**：
使用任务已保存的媒体分片重新执行合并处理的操作。
