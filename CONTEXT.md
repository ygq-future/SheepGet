# SheepGet

SheepGet 的下载交互领域术语。

## Language

**文件信息窗口**：
独立于主界面的桌面辅助窗口（Utility Window），在主界面手动新建、浏览器扩展接管或剪贴板监视捕获下载链接时弹出；展示资源信息、提供改名改目录及并发数设置；支持单例队列依次确认；在主窗口最小化至托盘时可独立唤起与关闭，不触发主窗口弹出。
_Avoid_: 文件信息对话框, 文件信息面板

**交接确认**：
浏览器扩展检测到下载意图后将请求上下文提交给桌面端，桌面端成功接收并入队文件信息窗口队列后向扩展返回的回执；区别于用户在文件信息窗口中点击确认的“用户最终确认”。扩展只有在收到交接确认后才终止浏览器原生下载。
_Avoid_: 最终确认, 接收完成

**候选资源**：
已完成交接确认、但尚未得到用户最终确认的下载任务；在文件信息窗口取消或连接断开时，系统将依规则清理其临时预占资源。

**环回通信通道**：
桌面端在本地回环地址（`127.0.0.1`）开启的安全通信服务，承担扩展状态探测、配置同步与下载交接；基于随机生成的会话令牌进行鉴权，作为桌面端与扩展之间的唯一通信通道。端点路径、鉴权头、查询参数、默认端口与顺延步长、事件名与扩展 ID 只在 `internal/protocol` 定义一次，扩展与前端侧的取值来自生成器产出的镜像，不另写字面量。
_Avoid_: 宿主桥接, 外部跳板

**端口顺延自愈**：
桌面端遇默认端口被占用时自动递增绑定可用端口，扩展结合当前目标端口与动态顺延范围自动重连并更新目标端口的连接自愈机制。
_Avoid_: 动态端口重试, 宿主拉起

**显示文件信息时立即下载**：
允许文件信息窗口保持打开时在后台开始下载的设置；用户仍可编辑保存信息并确认下载。
**媒体悬浮条**：
网页中关联可下载媒体资源的紧凑操作入口，用户取消下载后仍保持可用，并可手动关闭。

**暂停任务**：
停止传输但仍保留在下载列表中的任务，包括提前下载后用户取消文件信息窗口所保留的任务。

**内置分类**：
软件预置的文件归类规则与保存目录配置，其后缀规则可与自定义分类重叠。

**自定义分类**：
用户创建并配置后缀规则及保存目录的文件分类，匹配优先级高于内置分类；按用户可拖动调整的列表顺序取首个匹配项，新增分类置顶。

**目标落点**：
由文件名与当前配置判定出的保存结果，包含命中的分类与保存目录。判定只在后端进行一次，界面读取该结果而不自行实现分类规则；用户手动指定的目录优先于分类目录，但不改变命中的分类。
_Avoid_: 保存路径, 目标路径

**重复链接**：
与已有下载任务的完整 URL 相同的链接。

**重复链接策略**：
配置中心内定义的全局重复链接处理规则，支持四种模式：

1. **询问**（默认）：弹出提示，提供“跳过并显示完成信息”、“继续下载并覆盖”、“序号副本”三种选项由用户选择；
2. **跳过并显示完成信息**：直接展示对应已完成任务的完成信息，不创建新下载；目标保存位置已有成品文件时，文件信息窗口默认选中“查看已完成”；
3. **继续下载并覆盖**：弹出文件信息窗口确认，确认后对已有任务进行继续或重新覆盖；
4. **序号副本**：目标保存位置已有成品文件、确有需要避让时，弹出文件信息窗口并自动填充 `name (n).ext` 序号建议，确认后建立独立副本任务。

**成品文件**：
一次下载完成后落在目标保存位置的最终文件，区别于下载过程中的分片文件；重复链接裁决以它是否存在于目标位置区分局面。
_Avoid_: 完成文件, 目标文件

**重复链接裁决**：
判断一次下载遇到重复链接时该给用户哪些动作、默认执行哪一个的过程。输入是全局重复链接策略、是否存在同一 URL 的历史任务、该历史任务是否已完成、目标位置是否已有成品文件；输出是可选动作、默认动作，以及是否需要用户先选定一项。规则只在后端实现一次，文件信息窗口仅渲染裁决给出的选项并原样回传用户选中的动作，不依据策略自行推导。
_Avoid_: 重复策略判断, 重复处理逻辑

**同名文件冲突**：
新下载的目标保存位置已经有东西占着同名文件的情况。占用来源只有三类：磁盘上的成品文件或 `.sheepget` 中转文件、同目录同名且正在进行（排队中/下载中/处理中）的任务、以及已经登记进文件信息窗口队列的另一项；已完成但成品文件已不在磁盘上的历史记录不算占用。与重复链接相互独立，即使没有同一 URL 的历史任务也会发生。冲突提示与序号副本建议出自同一份判定，规则只在后端实现一次；窗口正在编辑的那一项不算占着自己的名字。
_Avoid_: 文件已存在, 路径冲突

**配置中心**：
管理全局偏好与运行规则的单一事实来源。管理外观主题、强调色、重复链接策略、默认并发上限、默认保存目录与分类规则；支持安装版与便携版两种数据存储模式。

**便携模式**：
数据存储模式之一。当程序可执行文件同级目录存在便携标志（如 `data/` 目录或 `portable` 标记文件）时生效，所有配置、任务数据库和日志保存在应用当前目录下，而非系统用户目录。

**安装模式**：
数据存储模式之一。配置与运行数据按操作系统惯例保存在平台标准用户数据目录（Windows `%APPDATA%`, macOS `~/Library/Application Support`, Linux `XDG_DATA_HOME`）。
**下载进度窗口**：
独立于主界面、集中展示多个任务实时下载区块和进度的共享窗口；每个任务完成后，其展示区域转为完成信息。

**完成信息**：
下载进度窗口中已完成任务的展示区域，提供打开文件、打开文件夹和关闭该区域的操作。

**失败阶段**：
任务契约中记录本次失败发生在传输还是处理的事实，决定界面提供哪种恢复操作；由产生失败的一方写入，不从错误文案推断。
_Avoid_: 错误类型, 失败原因

**处理失败**：
媒体分片已下载，但生成成品文件的合并处理失败的任务状态；保留分片以供重试处理。由失败阶段记录，不从错误文案推断。

**重试处理**：
使用任务已保存的媒体分片重新执行合并处理的操作，不重新传输；仅对处理阶段失败的任务有效。
_Avoid_: 重新下载
